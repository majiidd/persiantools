language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev"

install:
  - pip install coveralls
  - pip install -r requirements.txt

script:
  nosetests --with-coverage

after_success:
  coveralls